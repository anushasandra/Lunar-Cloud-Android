<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/feed_bg_color" >

    <TextView
        android:id="@+id/text_of_query"
        android:layout_width="fill_parent"
        android:layout_height="30dp"
        android:layout_marginLeft="7dp"
        android:layout_marginRight="7dp"
        android:layout_marginTop="7dp"
        android:background="@color/image_text_color"
        android:textColor="@color/black" />

    <ListView
        android:id="@id/listView1_result_page"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/text_of_query"
        android:layout_margin="7dp"
        android:background="@color/image_text_color"
        android:divider="@color/feed_bg_color"
        android:dividerHeight="10dp" >
    </ListView>

</RelativeLayout>